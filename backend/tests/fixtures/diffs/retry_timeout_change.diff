diff --git a/backend/app/providers/anthropic.py b/backend/app/providers/anthropic.py
index eeeeeee..fffffff 100644
--- a/backend/app/providers/anthropic.py
+++ b/backend/app/providers/anthropic.py
@@ -44,7 +44,7 @@ class AnthropicProvider(LLMProvider):
         api_key: str,
         model: str,
-        timeout: float = 60.0,
-        max_retries: int = 2,
+        timeout: float = 30.0,
+        max_retries: int = 5,
     ):
         self._client = anthropic.Anthropic(
             api_key=api_key,
             timeout=timeout,
-            max_retries=max_retries,
+            max_retries=max_retries,
+            # widen backoff window for flaky upstream connections
         )
