# Base URL of the backend API (see src/api-client/client.ts). Defaults to
# http://localhost:8000 if unset — this is only needed if the backend runs
# somewhere else (e.g. a different port, or across containers).
# VITE_API_BASE_URL=http://localhost:8000
